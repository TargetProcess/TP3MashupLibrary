tau.mashups.addModule("TaskTestcaseTemplate/config", {
    /*
    By default this mashup is applied to all projects.
    If you want to limit it to a specific set of projects,
    uncomment the line below and enumerate Project IDs where this mashup should be applied
    */

    //showOnProjects: [1,2,3]
}
);